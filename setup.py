from setuptools import setup

if __name__ == '__main__':
    setup(
        name='linksight-api-client',
        version='0.1',
        py_modules=['linksight'],
        install_requires=['requests'],
    )
